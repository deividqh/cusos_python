{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Terminal de depuración de JavaScript",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }    ]
}